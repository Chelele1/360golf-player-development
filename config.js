// Optional central database connection.
// Leave blank to use local browser storage + CSV/JSON export only.
// After deploying google-apps-script.gs as a Google Apps Script Web App,
// paste the /exec URL below.

window.GAMYPLAN_CONFIG = {
  googleSheetsWebAppUrl: ""
};
